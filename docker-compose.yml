services:
  # lumenwalk:
  #   build: .
  #   container_name: lumenwalk
  #   ports:
  #     - "3000:3000"     # run in development
  #   environment:
  #     - NODE_ENV=development

  prometheus:
    image: prom/prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana
    ports:
      - "3001:3000"
    volumes:
      - grafana-storage:/var/lib/grafana

  

volumes:
  grafana-storage:
